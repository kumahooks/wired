package whichkey

// Layout constants for the whichkey card.
const (
	// cardTopPadding is the blank space above the first action row, from the card's Padding(1, 2, 0, 2).
	cardTopPadding = 1

	// columnGap is the blank space between action columns on the same row.
	columnGap = 2

	// hintGapLines is the number of blank lines between the action rows and the close hint on the card's last line.
	hintGapLines = 1

	// zIndex is the card's z-order in the overlay compositor.
	zIndex = 10
)
